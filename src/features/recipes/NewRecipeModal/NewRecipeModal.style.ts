// Global Imports
import styled from "styled-components";

export const NewRecipeModalWrapper = styled.div`
  padding: 0.5rem;

  .close-button-wrapper {
    display: flex;
    justify-content: flex-end;
    margin: 0.5rem 0.5rem 0 0;
  }

  .post-success-wrapper {
    .button-wrapper {
      margin: 1rem 0 0 0;
      max-width: 33%;
    }
  }
`;
