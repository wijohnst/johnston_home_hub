import {
  Placement as BootstrapPlacement,
  Variant as BootstrapVariant,
} from "react-bootstrap/esm/types";

export type DefaultResponse = {
  status: number;
  message: string;
};

export type { BootstrapPlacement, BootstrapVariant };
